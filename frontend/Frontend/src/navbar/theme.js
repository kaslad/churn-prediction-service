import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    text: {
      primary: '#000000', // Set your primary text color here
    },
    // Define other colors in your palette
  },
});

export default theme;